using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercicio01A : MonoBehaviour
{
    int num = 10;
    float dec = 19f;
    bool file = false;
    string word = "Hola";

    // Start is called before the first frame update
    void Start()
    {
        print(num);
        print(dec);
        print(file);
        print(word);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
